spring.application.name=SKILLHIVE-backend

# MongoDB Configuration - Adding database name to the URI
spring.data.mongodb.uri=mongodb+srv://jayestore21:gZfiZU4cUV8v64oO@estd.coqyrsy.mongodb.net/?retryWrites=true&w=majority&appName=ESTD
# Server Configuration - Changed port from 8080 to 8081 to avoid conflict
server.port=8081

# Logging Configuration
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG

# CORS Configuration
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE
spring.web.cors.allowed-headers=*

# File upload configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=15MB
spring.servlet.multipart.file-size-threshold=2KB

# File storage configuration
file.upload-dir=./uploads
spring.web.resources.static-locations=file:${file.upload-dir}/,classpath:/static/
spring.mvc.static-path-pattern=/uploads/**

# Content negotiation
spring.mvc.contentnegotiation.favor-parameter=true
spring.mvc.contentnegotiation.media-types.json=application/json

# Multipart handling
spring.servlet.multipart.location=${java.io.tmpdir}
spring.servlet.multipart.resolve-lazily=true

# Debug options
debug=true
logging.level.org.springframework.web.servlet.mvc.method.annotation=TRACE
